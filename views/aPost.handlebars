<div class="row mb-4">
  <div class="col-md-5">
    <h2>
      <a href="/post/{{post.id}}">{{post.post_title}}</a>
    </h2>
    <p>
      <span class="">{{post.user_id}}</span><!-- need to relate Posts user_id to 'User's id-->
      needed!
    </p>
    <p>Created by {{post.user.name}} on {{format_date post.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{post.post_body}}
    </p>
  </div>
</div>